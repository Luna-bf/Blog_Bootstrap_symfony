{% extends 'base.html.twig' %}

{% block title %}Nouvelle publication - Blog Bootstrap
{% endblock %}

{% block body %}
	<body class="forms-body">
		<div class="shadow">

			<section class="d-flex flex-column form-div" id="create-post-div">
				{{ form_start(createPostForm, { 'attr': {'class': 'w-50 d-flex flex-column justify-content-center rounded-3'} }) }}
				<h1 class="mb-5 text-center text-light">Créer une nouvelle publication</h1>

				{#
	            La date (DateTimeImmutable) sera envoyée par le contrôleur grâce au setter "setCreatedAt". Effectuer l'envoi de
				cette donnée côté serveur est plus sécurisé car l'utilisateur n'y aura jamais accès (envoyer cette donnée via un
				formulaire lui donnerais la possibilité de modifier une donnée sensible, ce qui n'est pas désirable).
				#}

				{# START TITLE #}
				<div class="d-flex flex-column form-parent-row mb-4">
					{{ form_label(createPostForm.title, null, {'label_attr': {'class': 'form-label text-white'}}) }}

					<div class="d-flex align-items-center text-danger list-unstyled error-message">
						{{ form_errors(createPostForm.title) }}
					</div>

					{{ form_widget(createPostForm.title, {'attr': {'class': 'form-control'}}) }}
				</div>
				{# END TITLE #}

				{# START CONTENT #}
				<div class="d-flex flex-column form-parent-row mb-4">
					{{ form_label(createPostForm.content, null, {'label_attr': {'class': 'form-label text-white'}}) }}

					<div class="d-flex align-items-center text-danger list-unstyled error-message">
						{{ form_errors(createPostForm.content) }}
					</div>

					{{ form_widget(createPostForm.content, {'attr': {'class': 'form-control'}}) }}
				</div>
				{# END CONTENT #}

				{# START CATEGORY #}
				<div class="d-flex flex-column form-parent-row mb-4">
					{{ form_label(createPostForm.category, null, {'label_attr': {'class': 'form-label text-white'}}) }}

					<div class="d-flex align-items-center text-danger list-unstyled error-message">
						{{ form_errors(createPostForm.category) }}
					</div>

					{{ form_widget(createPostForm.category, {'attr': {'class': 'form-control'}}) }}
				</div>
				{# END CATEGORY #}

				{# START IMAGE #}
				<div class="d-flex flex-column form-parent-row mb-4">
					{{ form_label(createPostForm.image_name, null, {'label_attr': {'class': 'form-label text-white'}}) }}

					<div class="d-flex align-items-center text-danger list-unstyled error-message">
						{{ form_errors(createPostForm.image_name) }}
					</div>

					{{ form_widget(createPostForm.image_name, {'attr': {'class': 'form-control'}}) }}
				</div>
				{# END IMAGE #}
				
				{{ form_row(createPostForm.submit, {'row_attr': {'class': 'mb-4 d-flex justify-content-center'}, 'attr': {'class': 'btn align-self-start mt-3 w-50 m-auto text-white bg-dark'}}) }}

				{{ form_end(createPostForm) }}
			</section>
		</div>
	</body>
{% endblock %}

{% block footer %}
	<footer class="bg-dark text-light d-flex align-items-center justify-content-center">
		<p class="m-0 text-center">Travail réalisé par
			<a href="https://github.com/Luna-bf" target="_blank" class="text-decoration-none">Luna</a>.</p>
	</footer>
{% endblock %}