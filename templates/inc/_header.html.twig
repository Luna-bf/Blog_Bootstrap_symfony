{% block header %}
	{# <HEADER> #}
	<header class="px-3 bg-dark w-100 sticky-top">
		{# <NAV> #}
		<nav class="row d-flex flex-wrap align-items-center">
			{# <UL> #}
			<ul class="list-unstyled d-flex justify-content-between m-0">
				{# <LI> (Blog Bootstrap) #}
				<li class="text-center">
					<a class="blog-header-logo fw-semibold fs-5 text-white text-decoration-none" href="{{ path('post_index') }}">Blog Bootstrap</a>
				</li>
				{# FIN <LI> (Blog Bootstrap) #}
				
				{# <LI> (User) #}
				<li class="col-4 d-flex justify-content-end align-items-center">
					{% if is_granted(("ROLE_USER")) %}
						<a class="text-white text-decoration-none" href="{{ path('user_index') }}">Bonjour,
							{{ app.user.username }}</a>
					{% else %}
						<a class="text-white text-decoration-none" href="{{ path('app_register') }}">Inscription</a>
						<a class="text-white text-decoration-none ms-3" href="{{ path('app_login') }}">Connexion</a>
					{% endif %}
				</li>
				{# FIN <LI> (User) #}
			</ul>
			{# FIN <UL> #}
		</nav>
		{# FIN <NAV> #}
	</header>
	{# FIN <HEADER> #}
{% endblock %}
