{% block header %}
	<header class="container">
		<nav class="border-bottom lh-1 py-3">
			<div class="row flex-nowrap justify-content-between align-items-center">
				<div class="col-4 pt-1">
					<a class="link-secondary" href="#">Subscribe</a>
				</div>
				<div class="col-4 text-center">
					<a class="blog-header-logo text-body-emphasis text-decoration-none" href="#">Bootstrap Blog</a>
				</div>
				<div class="col-4 d-flex justify-content-end align-items-center">
					<a class="text-secondary text-decoration-none" href="{{ path('post_index') }}">Accueil</a>
					{# <a class="btn btn-sm btn-outline-secondary ms-3" href="{{ path('app_logout') }}">Déconnexion</a> #}
				</div>
			</div>
		</nav>
	</header>
{% endblock %}
